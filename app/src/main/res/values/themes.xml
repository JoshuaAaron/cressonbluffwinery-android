<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CressonBluffWinery" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        d
        <item name="colorOnPrimary">@color/colorNavBarText</item>
        <!-- Secondary brand color. -->
        <item name="colorAccent">@color/colorTextDark</item>
        <item name="colorSecondaryVariant">@color/colorText</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.CressonBluffWinery.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.CressonBluffWinery.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.CressonBluffWinery.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>